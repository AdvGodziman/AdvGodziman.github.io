document.getElementById('test2Button').addEventListener('click', function() {
    // Add code to perform the login process here (e.g., check credentials).

    // Assuming the login is successful, navigate back to the home page.
    window.location.href = 'home.html';
});